name: Schedule Workflow

on:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  run-message:
    runs-on: ubuntu-latest
    steps:
      - name: Mensaje programado
        run: echo "Ejecución programada"
