name: Pull Request Workflow

on:
  pull_request:
    branches:
      - main

jobs:
  run-message:
    runs-on: ubuntu-latest
    steps:
      - name: Mensaje por PR
        run: echo "Pull request detectado"
